extend ../../_component/layout/base.pug


//-  hr tag
h4  Aici am folosit hr element in html
p
  | HTML is the standard markup language for creating Web pages. HTML describes the structure of a Web page,
  | and consists of a series of elements. HTML elements tell the browser how to display the content.
hr
p
  | CSS is a language that describes how HTML elements are to be displayed on screen, paper, or in other
  | media. CSS saves a lot of work, because it can control the layout of multiple web pages all at once.
hr
p
  | JavaScript is the programming language of HTML and the Web. JavaScript can change HTML content and
  | attribute values. JavaScript can change CSS. JavaScript can hide and show HTML elements, and more.


//-  progress tag  cu atributele  value si max
h4 Acesta este progress element cu atributul value de 32% si atributul max de 100
label(for='file') Downloading progress:
progress#file(value='32' max='100')  32%
